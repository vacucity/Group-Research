[phases.setup]
nixPkgs = ["python311Full"]

[phases.install]
cmds = ["python3 -m pip install -r ai-service/requirements.txt"]

[start]
cmd = "cd ai-service && python3 -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
